{
  "name": "transactions-app",
  "version": "1.0.0",
  "description": "A full-stack transactions app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "cd frontend && npm start",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "engines": {
    "node": ">=14"
  },
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ]
}
